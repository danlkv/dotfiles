# If a config.py file exists, this file is ignored unless it's explicitly loaded
# via config.load_autoconfig(). For more information, see:
# https://github.com/qutebrowser/qutebrowser/blob/master/doc/help/configuring.asciidoc#loading-autoconfigyml
# DO NOT edit this file by hand, qutebrowser will overwrite it.
# Instead, create a config.py - see :help for details.

config_version: 2
settings:
  bindings.commands:
    global:
      insert:
        <Ctrl+m>: fake-key <return>
      normal:
        ',j': :run-with-count 5 scroll down
        ',m': spawn mpv {url}
        <Ctrl+Shift+h>: :open qute://history
        <Ctrl+f>: :hint all tab-bg --rapid
        <Ctrl+h>: :open qute://history
        <Ctrl+m>: fake-key
        <Ctrl+o>: download-open
        ft: '`:hint all tab-bg --rapid`'
        jj: 4j
        sR: :spawn --userscript speed_reader
        tL: open https://outline.com/{url}
        tR: :spawn --userscript readability
        u: hint
  content.notifications:
    https://www.reddit.com: false
  content.pdfjs:
    global: true
  hints.auto_follow_timeout:
    global: 400
  hints.chars:
    global: uhetonasmj
  hints.dictionary:
    global: /usr/share/dict/cracklib-small
  hints.mode:
    global: word
  hints.scatter:
    global: false
  history_gap_interval:
    global: 30
  input.partial_timeout:
    global: 5000
  input.spatial_navigation:
    global: false
  keyhint.blacklist:
    global:
    - f*
    - .*
    - y*
    - l*
    - b*
    - x*
    - k*
    - q*
  keyhint.delay:
    global: 0
  tabs.position:
    global: top
  url.searchengines:
    global:
      DEFAULT: https://google.com/search?q={}
      DUCK: https://duckduckgo.com/?q={}
